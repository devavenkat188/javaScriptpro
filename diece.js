let player = (Math.floor(Math.random()*6));
let value = 100;
let pos;
let result;
if(player === 1) {
    result = "Enter into the Game";
    if (player <= 6 && value != 0) {
        pos = value - player;
    }
    else if (player > 6 && value != 0) {
        pos = value - player;
    }
    else if (player > 6 && value != 0) {
        pos = value - player;
    }
    else if (player > 6 && value != 0) {
        pos = value - player;
    }
}
