const employee = {
    firstName: 'Devashankar',
    lastName: 'V',
    age: 25,
    isMajor: true,
};

const{firstName} = employee;
console.log(employee.firstName);
console.log(firstName); // because the objedt  is created in the above line.