
let getFullName = function(firstName,lastName) { // the entire function will be stored in a single variable of getFull
    // Name --> Anonymous function.
    let name = firstName+' '+lastName;
    return name;
}
let fullName = getFullName('Deva','V');
console.log(fullName);