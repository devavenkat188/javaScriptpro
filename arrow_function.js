/* 
const getFullName = (firstName,lastName) => { // if has multiple lines use this method.
    return (firstName+" "+lastName);
}
*/
const getFullName = (firstName,lastName) => firstName+" "+lastName; // if the function has only one line
// the statement will be written like this.
let fullName = getFullName("Devashankar","V");
console.log(fullName);