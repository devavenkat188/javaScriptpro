let age = 25;
let newAge = age;
age ++;
console.log("Age : "+age); // new incremented value
console.log("newAge : "+newAge); // remains the same initial value.

/* In primitve datatype the deep copy object is achieved by assign the one variable directly to the another variable
at initial what the value given to the 1st variable is remain constant in the assigning of another variable.
here the first variable value is changed & second variable value remains unchanged & holds the same value at
initial first variable value.
*/
